/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import client from '@kubb/plugin-client/clients/axios';
import type {
  ListRunsQueryResponse,
  ListRunsQueryParams,
  ListRuns422,
} from '../../types/ListRuns.ts';
import type {
  RequestConfig,
  ResponseErrorConfig,
  ResponseConfig,
} from '@kubb/plugin-client/clients/axios';
import type {
  InfiniteData,
  QueryKey,
  QueryClient,
  InfiniteQueryObserverOptions,
  UseInfiniteQueryResult,
} from '@tanstack/react-query';
import { infiniteQueryOptions, useInfiniteQuery } from '@tanstack/react-query';

export const listRunsInfiniteQueryKey = (params?: ListRunsQueryParams) =>
  [{ url: '/v1/runs/' }, ...(params ? [params] : [])] as const;

export type ListRunsInfiniteQueryKey = ReturnType<
  typeof listRunsInfiniteQueryKey
>;

/**
 * @description List all runs.
 * @summary List Runs
 * {@link /v1/runs/}
 */
export async function listRunsInfinite(
  params?: ListRunsQueryParams,
  config: Partial<RequestConfig> & { client?: typeof client } = {}
) {
  const { client: request = client, ...requestConfig } = config;

  const res = await request<
    ListRunsQueryResponse,
    ResponseErrorConfig<ListRuns422>,
    unknown
  >({ method: 'GET', url: `/v1/runs/`, params, ...requestConfig });
  return res;
}

export function listRunsInfiniteQueryOptions(
  params?: ListRunsQueryParams,
  config: Partial<RequestConfig> & { client?: typeof client } = {}
) {
  const queryKey = listRunsInfiniteQueryKey(params);
  return infiniteQueryOptions<
    ResponseConfig<ListRunsQueryResponse>,
    ResponseErrorConfig<ListRuns422>,
    ResponseConfig<ListRunsQueryResponse>,
    typeof queryKey,
    number
  >({
    queryKey,
    queryFn: async ({ signal, pageParam }) => {
      config.signal = signal;

      if (params) {
        params['next_page'] =
          pageParam as unknown as ListRunsQueryParams['next_page'];
      }
      return listRunsInfinite(params, config);
    },
    initialPageParam: 0,
    getNextPageParam: (lastPage) => lastPage['nextCursor'],
    getPreviousPageParam: (firstPage) => firstPage['nextCursor'],
  });
}

/**
 * @description List all runs.
 * @summary List Runs
 * {@link /v1/runs/}
 */
export function useListRunsInfinite<
  TData = InfiniteData<ResponseConfig<ListRunsQueryResponse>>,
  TQueryData = ResponseConfig<ListRunsQueryResponse>,
  TQueryKey extends QueryKey = ListRunsInfiniteQueryKey
>(
  params?: ListRunsQueryParams,
  options: {
    query?: Partial<
      InfiniteQueryObserverOptions<
        ResponseConfig<ListRunsQueryResponse>,
        ResponseErrorConfig<ListRuns422>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & {
      client?: QueryClient;
    };
    client?: Partial<RequestConfig> & { client?: typeof client };
  } = {}
) {
  const {
    query: { client: queryClient, ...queryOptions } = {},
    client: config = {},
  } = options ?? {};
  const queryKey = queryOptions?.queryKey ?? listRunsInfiniteQueryKey(params);

  const query = useInfiniteQuery(
    {
      ...(listRunsInfiniteQueryOptions(
        params,
        config
      ) as unknown as InfiniteQueryObserverOptions),
      queryKey,
      ...(queryOptions as unknown as Omit<
        InfiniteQueryObserverOptions,
        'queryKey'
      >),
    },
    queryClient
  ) as UseInfiniteQueryResult<TData, ResponseErrorConfig<ListRuns422>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
