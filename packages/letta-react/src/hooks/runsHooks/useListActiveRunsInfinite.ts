/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import client from '@kubb/plugin-client/clients/axios';
import type {
  ListActiveRunsQueryResponse,
  ListActiveRunsQueryParams,
  ListActiveRuns422,
} from '../../types/ListActiveRuns.ts';
import type {
  RequestConfig,
  ResponseErrorConfig,
  ResponseConfig,
} from '@kubb/plugin-client/clients/axios';
import type {
  InfiniteData,
  QueryKey,
  QueryClient,
  InfiniteQueryObserverOptions,
  UseInfiniteQueryResult,
} from '@tanstack/react-query';
import { infiniteQueryOptions, useInfiniteQuery } from '@tanstack/react-query';

export const listActiveRunsInfiniteQueryKey = (
  params?: ListActiveRunsQueryParams
) => [{ url: '/v1/runs/active' }, ...(params ? [params] : [])] as const;

export type ListActiveRunsInfiniteQueryKey = ReturnType<
  typeof listActiveRunsInfiniteQueryKey
>;

/**
 * @description List all active runs.
 * @summary List Active Runs
 * {@link /v1/runs/active}
 */
export async function listActiveRunsInfinite(
  params?: ListActiveRunsQueryParams,
  config: Partial<RequestConfig> & { client?: typeof client } = {}
) {
  const { client: request = client, ...requestConfig } = config;

  const res = await request<
    ListActiveRunsQueryResponse,
    ResponseErrorConfig<ListActiveRuns422>,
    unknown
  >({
    method: 'GET',
    url: `/v1/runs/active`,
    params,
    ...requestConfig,
  });
  return res;
}

export function listActiveRunsInfiniteQueryOptions(
  params?: ListActiveRunsQueryParams,
  config: Partial<RequestConfig> & { client?: typeof client } = {}
) {
  const queryKey = listActiveRunsInfiniteQueryKey(params);
  return infiniteQueryOptions<
    ResponseConfig<ListActiveRunsQueryResponse>,
    ResponseErrorConfig<ListActiveRuns422>,
    ResponseConfig<ListActiveRunsQueryResponse>,
    typeof queryKey,
    number
  >({
    queryKey,
    queryFn: async ({ signal, pageParam }) => {
      config.signal = signal;

      if (params) {
        params['next_page'] =
          pageParam as unknown as ListActiveRunsQueryParams['next_page'];
      }
      return listActiveRunsInfinite(params, config);
    },
    initialPageParam: 0,
    getNextPageParam: (lastPage) => lastPage['nextCursor'],
    getPreviousPageParam: (firstPage) => firstPage['nextCursor'],
  });
}

/**
 * @description List all active runs.
 * @summary List Active Runs
 * {@link /v1/runs/active}
 */
export function useListActiveRunsInfinite<
  TData = InfiniteData<ResponseConfig<ListActiveRunsQueryResponse>>,
  TQueryData = ResponseConfig<ListActiveRunsQueryResponse>,
  TQueryKey extends QueryKey = ListActiveRunsInfiniteQueryKey
>(
  params?: ListActiveRunsQueryParams,
  options: {
    query?: Partial<
      InfiniteQueryObserverOptions<
        ResponseConfig<ListActiveRunsQueryResponse>,
        ResponseErrorConfig<ListActiveRuns422>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: typeof client };
  } = {}
) {
  const {
    query: { client: queryClient, ...queryOptions } = {},
    client: config = {},
  } = options ?? {};
  const queryKey =
    queryOptions?.queryKey ?? listActiveRunsInfiniteQueryKey(params);

  const query = useInfiniteQuery(
    {
      ...(listActiveRunsInfiniteQueryOptions(
        params,
        config
      ) as unknown as InfiniteQueryObserverOptions),
      queryKey,
      ...(queryOptions as unknown as Omit<
        InfiniteQueryObserverOptions,
        'queryKey'
      >),
    },
    queryClient
  ) as UseInfiniteQueryResult<TData, ResponseErrorConfig<ListActiveRuns422>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
