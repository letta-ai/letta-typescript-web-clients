/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import client from '@kubb/plugin-client/clients/axios';
import type {
  ListSourceFilesQueryResponse,
  ListSourceFilesPathParams,
  ListSourceFilesQueryParams,
  ListSourceFiles422,
} from '../../types/ListSourceFiles.ts';
import type {
  RequestConfig,
  ResponseErrorConfig,
  ResponseConfig,
} from '@kubb/plugin-client/clients/axios';
import type {
  InfiniteData,
  QueryKey,
  QueryClient,
  InfiniteQueryObserverOptions,
  UseInfiniteQueryResult,
} from '@tanstack/react-query';
import { infiniteQueryOptions, useInfiniteQuery } from '@tanstack/react-query';

export const listSourceFilesInfiniteQueryKey = (
  source_id: ListSourceFilesPathParams['source_id'],
  params?: ListSourceFilesQueryParams
) =>
  [
    { url: '/v1/sources/:source_id/files', params: { source_id: source_id } },
    ...(params ? [params] : []),
  ] as const;

export type ListSourceFilesInfiniteQueryKey = ReturnType<
  typeof listSourceFilesInfiniteQueryKey
>;

/**
 * @description List paginated files associated with a data source.
 * @summary List Source Files
 * {@link /v1/sources/:source_id/files}
 */
export async function listSourceFilesInfinite(
  source_id: ListSourceFilesPathParams['source_id'],
  params?: ListSourceFilesQueryParams,
  config: Partial<RequestConfig> & { client?: typeof client } = {}
) {
  const { client: request = client, ...requestConfig } = config;

  const res = await request<
    ListSourceFilesQueryResponse,
    ResponseErrorConfig<ListSourceFiles422>,
    unknown
  >({
    method: 'GET',
    url: `/v1/sources/${source_id}/files`,
    params,
    ...requestConfig,
  });
  return res;
}

export function listSourceFilesInfiniteQueryOptions(
  source_id: ListSourceFilesPathParams['source_id'],
  params?: ListSourceFilesQueryParams,
  config: Partial<RequestConfig> & { client?: typeof client } = {}
) {
  const queryKey = listSourceFilesInfiniteQueryKey(source_id, params);
  return infiniteQueryOptions<
    ResponseConfig<ListSourceFilesQueryResponse>,
    ResponseErrorConfig<ListSourceFiles422>,
    ResponseConfig<ListSourceFilesQueryResponse>,
    typeof queryKey,
    number
  >({
    enabled: !!source_id,
    queryKey,
    queryFn: async ({ signal, pageParam }) => {
      config.signal = signal;

      if (params) {
        params['next_page'] =
          pageParam as unknown as ListSourceFilesQueryParams['next_page'];
      }
      return listSourceFilesInfinite(source_id, params, config);
    },
    initialPageParam: 0,
    getNextPageParam: (lastPage) => lastPage['nextCursor'],
    getPreviousPageParam: (firstPage) => firstPage['nextCursor'],
  });
}

/**
 * @description List paginated files associated with a data source.
 * @summary List Source Files
 * {@link /v1/sources/:source_id/files}
 */
export function useListSourceFilesInfinite<
  TData = InfiniteData<ResponseConfig<ListSourceFilesQueryResponse>>,
  TQueryData = ResponseConfig<ListSourceFilesQueryResponse>,
  TQueryKey extends QueryKey = ListSourceFilesInfiniteQueryKey
>(
  source_id: ListSourceFilesPathParams['source_id'],
  params?: ListSourceFilesQueryParams,
  options: {
    query?: Partial<
      InfiniteQueryObserverOptions<
        ResponseConfig<ListSourceFilesQueryResponse>,
        ResponseErrorConfig<ListSourceFiles422>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: typeof client };
  } = {}
) {
  const {
    query: { client: queryClient, ...queryOptions } = {},
    client: config = {},
  } = options ?? {};
  const queryKey =
    queryOptions?.queryKey ??
    listSourceFilesInfiniteQueryKey(source_id, params);

  const query = useInfiniteQuery(
    {
      ...(listSourceFilesInfiniteQueryOptions(
        source_id,
        params,
        config
      ) as unknown as InfiniteQueryObserverOptions),
      queryKey,
      ...(queryOptions as unknown as Omit<
        InfiniteQueryObserverOptions,
        'queryKey'
      >),
    },
    queryClient
  ) as UseInfiniteQueryResult<
    TData,
    ResponseErrorConfig<ListSourceFiles422>
  > & { queryKey: TQueryKey };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
