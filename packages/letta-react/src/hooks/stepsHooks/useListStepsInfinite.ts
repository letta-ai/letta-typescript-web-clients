/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import client from '@kubb/plugin-client/clients/axios';
import type {
  ListStepsQueryResponse,
  ListStepsQueryParams,
  ListSteps422,
} from '../../types/ListSteps.ts';
import type {
  RequestConfig,
  ResponseErrorConfig,
  ResponseConfig,
} from '@kubb/plugin-client/clients/axios';
import type {
  InfiniteData,
  QueryKey,
  QueryClient,
  InfiniteQueryObserverOptions,
  UseInfiniteQueryResult,
} from '@tanstack/react-query';
import { infiniteQueryOptions, useInfiniteQuery } from '@tanstack/react-query';

export const listStepsInfiniteQueryKey = (params?: ListStepsQueryParams) =>
  [{ url: '/v1/steps' }, ...(params ? [params] : [])] as const;

export type ListStepsInfiniteQueryKey = ReturnType<
  typeof listStepsInfiniteQueryKey
>;

/**
 * @description List steps with optional pagination and date filters.Dates should be provided in ISO 8601 format (e.g. 2025-01-29T15:01:19-08:00)
 * @summary List Steps
 * {@link /v1/steps}
 */
export async function listStepsInfinite(
  params?: ListStepsQueryParams,
  config: Partial<RequestConfig> & { client?: typeof client } = {}
) {
  const { client: request = client, ...requestConfig } = config;

  const res = await request<
    ListStepsQueryResponse,
    ResponseErrorConfig<ListSteps422>,
    unknown
  >({ method: 'GET', url: `/v1/steps`, params, ...requestConfig });
  return res;
}

export function listStepsInfiniteQueryOptions(
  params?: ListStepsQueryParams,
  config: Partial<RequestConfig> & { client?: typeof client } = {}
) {
  const queryKey = listStepsInfiniteQueryKey(params);
  return infiniteQueryOptions<
    ResponseConfig<ListStepsQueryResponse>,
    ResponseErrorConfig<ListSteps422>,
    ResponseConfig<ListStepsQueryResponse>,
    typeof queryKey,
    number
  >({
    queryKey,
    queryFn: async ({ signal, pageParam }) => {
      config.signal = signal;

      if (params) {
        params['next_page'] =
          pageParam as unknown as ListStepsQueryParams['next_page'];
      }
      return listStepsInfinite(params, config);
    },
    initialPageParam: 0,
    getNextPageParam: (lastPage) => lastPage['nextCursor'],
    getPreviousPageParam: (firstPage) => firstPage['nextCursor'],
  });
}

/**
 * @description List steps with optional pagination and date filters.Dates should be provided in ISO 8601 format (e.g. 2025-01-29T15:01:19-08:00)
 * @summary List Steps
 * {@link /v1/steps}
 */
export function useListStepsInfinite<
  TData = InfiniteData<ResponseConfig<ListStepsQueryResponse>>,
  TQueryData = ResponseConfig<ListStepsQueryResponse>,
  TQueryKey extends QueryKey = ListStepsInfiniteQueryKey
>(
  params?: ListStepsQueryParams,
  options: {
    query?: Partial<
      InfiniteQueryObserverOptions<
        ResponseConfig<ListStepsQueryResponse>,
        ResponseErrorConfig<ListSteps422>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & {
      client?: QueryClient;
    };
    client?: Partial<RequestConfig> & { client?: typeof client };
  } = {}
) {
  const {
    query: { client: queryClient, ...queryOptions } = {},
    client: config = {},
  } = options ?? {};
  const queryKey = queryOptions?.queryKey ?? listStepsInfiniteQueryKey(params);

  const query = useInfiniteQuery(
    {
      ...(listStepsInfiniteQueryOptions(
        params,
        config
      ) as unknown as InfiniteQueryObserverOptions),
      queryKey,
      ...(queryOptions as unknown as Omit<
        InfiniteQueryObserverOptions,
        'queryKey'
      >),
    },
    queryClient
  ) as UseInfiniteQueryResult<TData, ResponseErrorConfig<ListSteps422>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
