/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import client from '@kubb/plugin-client/clients/axios';
import type {
  ListToolsQueryResponse,
  ListToolsQueryParams,
  ListTools422,
} from '../../types/ListTools.ts';
import type {
  RequestConfig,
  ResponseErrorConfig,
  ResponseConfig,
} from '@kubb/plugin-client/clients/axios';
import type {
  InfiniteData,
  QueryKey,
  QueryClient,
  InfiniteQueryObserverOptions,
  UseInfiniteQueryResult,
} from '@tanstack/react-query';
import { infiniteQueryOptions, useInfiniteQuery } from '@tanstack/react-query';

export const listToolsInfiniteQueryKey = (params?: ListToolsQueryParams) =>
  [{ url: '/v1/tools/' }, ...(params ? [params] : [])] as const;

export type ListToolsInfiniteQueryKey = ReturnType<
  typeof listToolsInfiniteQueryKey
>;

/**
 * @description Get a list of all tools available to agents belonging to the org of the user
 * @summary List Tools
 * {@link /v1/tools/}
 */
export async function listToolsInfinite(
  params?: ListToolsQueryParams,
  config: Partial<RequestConfig> & { client?: typeof client } = {}
) {
  const { client: request = client, ...requestConfig } = config;

  const res = await request<
    ListToolsQueryResponse,
    ResponseErrorConfig<ListTools422>,
    unknown
  >({ method: 'GET', url: `/v1/tools/`, params, ...requestConfig });
  return res;
}

export function listToolsInfiniteQueryOptions(
  params?: ListToolsQueryParams,
  config: Partial<RequestConfig> & { client?: typeof client } = {}
) {
  const queryKey = listToolsInfiniteQueryKey(params);
  return infiniteQueryOptions<
    ResponseConfig<ListToolsQueryResponse>,
    ResponseErrorConfig<ListTools422>,
    ResponseConfig<ListToolsQueryResponse>,
    typeof queryKey,
    number
  >({
    queryKey,
    queryFn: async ({ signal, pageParam }) => {
      config.signal = signal;

      if (params) {
        params['next_page'] =
          pageParam as unknown as ListToolsQueryParams['next_page'];
      }
      return listToolsInfinite(params, config);
    },
    initialPageParam: 0,
    getNextPageParam: (lastPage) => lastPage['nextCursor'],
    getPreviousPageParam: (firstPage) => firstPage['nextCursor'],
  });
}

/**
 * @description Get a list of all tools available to agents belonging to the org of the user
 * @summary List Tools
 * {@link /v1/tools/}
 */
export function useListToolsInfinite<
  TData = InfiniteData<ResponseConfig<ListToolsQueryResponse>>,
  TQueryData = ResponseConfig<ListToolsQueryResponse>,
  TQueryKey extends QueryKey = ListToolsInfiniteQueryKey
>(
  params?: ListToolsQueryParams,
  options: {
    query?: Partial<
      InfiniteQueryObserverOptions<
        ResponseConfig<ListToolsQueryResponse>,
        ResponseErrorConfig<ListTools422>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & {
      client?: QueryClient;
    };
    client?: Partial<RequestConfig> & { client?: typeof client };
  } = {}
) {
  const {
    query: { client: queryClient, ...queryOptions } = {},
    client: config = {},
  } = options ?? {};
  const queryKey = queryOptions?.queryKey ?? listToolsInfiniteQueryKey(params);

  const query = useInfiniteQuery(
    {
      ...(listToolsInfiniteQueryOptions(
        params,
        config
      ) as unknown as InfiniteQueryObserverOptions),
      queryKey,
      ...(queryOptions as unknown as Omit<
        InfiniteQueryObserverOptions,
        'queryKey'
      >),
    },
    queryClient
  ) as UseInfiniteQueryResult<TData, ResponseErrorConfig<ListTools422>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
