/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import client from '@kubb/plugin-client/clients/axios';
import type {
  ListMcpServersQueryResponse,
  ListMcpServersHeaderParams,
  ListMcpServers422,
} from '../../types/ListMcpServers.ts';
import type {
  RequestConfig,
  ResponseErrorConfig,
  ResponseConfig,
} from '@kubb/plugin-client/clients/axios';
import type {
  InfiniteData,
  QueryKey,
  QueryClient,
  InfiniteQueryObserverOptions,
  UseInfiniteQueryResult,
} from '@tanstack/react-query';
import { infiniteQueryOptions, useInfiniteQuery } from '@tanstack/react-query';

export const listMcpServersInfiniteQueryKey = () =>
  [{ url: '/v1/tools/mcp/servers' }] as const;

export type ListMcpServersInfiniteQueryKey = ReturnType<
  typeof listMcpServersInfiniteQueryKey
>;

/**
 * @description Get a list of all configured MCP servers
 * @summary List Mcp Servers
 * {@link /v1/tools/mcp/servers}
 */
export async function listMcpServersInfinite(
  headers?: ListMcpServersHeaderParams,
  config: Partial<RequestConfig> & { client?: typeof client } = {}
) {
  const { client: request = client, ...requestConfig } = config;

  const res = await request<
    ListMcpServersQueryResponse,
    ResponseErrorConfig<ListMcpServers422>,
    unknown
  >({
    method: 'GET',
    url: `/v1/tools/mcp/servers`,
    ...requestConfig,
    headers: { ...headers, ...requestConfig.headers },
  });
  return res;
}

export function listMcpServersInfiniteQueryOptions(
  headers?: ListMcpServersHeaderParams,
  config: Partial<RequestConfig> & { client?: typeof client } = {}
) {
  const queryKey = listMcpServersInfiniteQueryKey();
  return infiniteQueryOptions<
    ResponseConfig<ListMcpServersQueryResponse>,
    ResponseErrorConfig<ListMcpServers422>,
    ResponseConfig<ListMcpServersQueryResponse>,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      config.signal = signal;
      return listMcpServersInfinite(headers, config);
    },
    initialPageParam: 0,
    getNextPageParam: (lastPage) => lastPage['nextCursor'],
    getPreviousPageParam: (firstPage) => firstPage['nextCursor'],
  });
}

/**
 * @description Get a list of all configured MCP servers
 * @summary List Mcp Servers
 * {@link /v1/tools/mcp/servers}
 */
export function useListMcpServersInfinite<
  TData = InfiniteData<ResponseConfig<ListMcpServersQueryResponse>>,
  TQueryData = ResponseConfig<ListMcpServersQueryResponse>,
  TQueryKey extends QueryKey = ListMcpServersInfiniteQueryKey
>(
  headers?: ListMcpServersHeaderParams,
  options: {
    query?: Partial<
      InfiniteQueryObserverOptions<
        ResponseConfig<ListMcpServersQueryResponse>,
        ResponseErrorConfig<ListMcpServers422>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: typeof client };
  } = {}
) {
  const {
    query: { client: queryClient, ...queryOptions } = {},
    client: config = {},
  } = options ?? {};
  const queryKey = queryOptions?.queryKey ?? listMcpServersInfiniteQueryKey();

  const query = useInfiniteQuery(
    {
      ...(listMcpServersInfiniteQueryOptions(
        headers,
        config
      ) as unknown as InfiniteQueryObserverOptions),
      queryKey,
      ...(queryOptions as unknown as Omit<
        InfiniteQueryObserverOptions,
        'queryKey'
      >),
    },
    queryClient
  ) as UseInfiniteQueryResult<TData, ResponseErrorConfig<ListMcpServers422>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
