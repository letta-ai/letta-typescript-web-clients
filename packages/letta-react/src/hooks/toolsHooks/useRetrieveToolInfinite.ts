/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import client from '@kubb/plugin-client/clients/axios';
import type {
  RetrieveToolQueryResponse,
  RetrieveToolPathParams,
  RetrieveTool422,
} from '../../types/RetrieveTool.ts';
import type {
  RequestConfig,
  ResponseErrorConfig,
  ResponseConfig,
} from '@kubb/plugin-client/clients/axios';
import type {
  InfiniteData,
  QueryKey,
  QueryClient,
  InfiniteQueryObserverOptions,
  UseInfiniteQueryResult,
} from '@tanstack/react-query';
import { infiniteQueryOptions, useInfiniteQuery } from '@tanstack/react-query';

export const retrieveToolInfiniteQueryKey = (
  tool_id: RetrieveToolPathParams['tool_id']
) => [{ url: '/v1/tools/:tool_id', params: { tool_id: tool_id } }] as const;

export type RetrieveToolInfiniteQueryKey = ReturnType<
  typeof retrieveToolInfiniteQueryKey
>;

/**
 * @description Get a tool by ID
 * @summary Retrieve Tool
 * {@link /v1/tools/:tool_id}
 */
export async function retrieveToolInfinite(
  tool_id: RetrieveToolPathParams['tool_id'],
  config: Partial<RequestConfig> & { client?: typeof client } = {}
) {
  const { client: request = client, ...requestConfig } = config;

  const res = await request<
    RetrieveToolQueryResponse,
    ResponseErrorConfig<RetrieveTool422>,
    unknown
  >({
    method: 'GET',
    url: `/v1/tools/${tool_id}`,
    ...requestConfig,
  });
  return res;
}

export function retrieveToolInfiniteQueryOptions(
  tool_id: RetrieveToolPathParams['tool_id'],
  config: Partial<RequestConfig> & { client?: typeof client } = {}
) {
  const queryKey = retrieveToolInfiniteQueryKey(tool_id);
  return infiniteQueryOptions<
    ResponseConfig<RetrieveToolQueryResponse>,
    ResponseErrorConfig<RetrieveTool422>,
    ResponseConfig<RetrieveToolQueryResponse>,
    typeof queryKey
  >({
    enabled: !!tool_id,
    queryKey,
    queryFn: async ({ signal }) => {
      config.signal = signal;
      return retrieveToolInfinite(tool_id, config);
    },
    initialPageParam: 0,
    getNextPageParam: (lastPage) => lastPage['nextCursor'],
    getPreviousPageParam: (firstPage) => firstPage['nextCursor'],
  });
}

/**
 * @description Get a tool by ID
 * @summary Retrieve Tool
 * {@link /v1/tools/:tool_id}
 */
export function useRetrieveToolInfinite<
  TData = InfiniteData<ResponseConfig<RetrieveToolQueryResponse>>,
  TQueryData = ResponseConfig<RetrieveToolQueryResponse>,
  TQueryKey extends QueryKey = RetrieveToolInfiniteQueryKey
>(
  tool_id: RetrieveToolPathParams['tool_id'],
  options: {
    query?: Partial<
      InfiniteQueryObserverOptions<
        ResponseConfig<RetrieveToolQueryResponse>,
        ResponseErrorConfig<RetrieveTool422>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: typeof client };
  } = {}
) {
  const {
    query: { client: queryClient, ...queryOptions } = {},
    client: config = {},
  } = options ?? {};
  const queryKey =
    queryOptions?.queryKey ?? retrieveToolInfiniteQueryKey(tool_id);

  const query = useInfiniteQuery(
    {
      ...(retrieveToolInfiniteQueryOptions(
        tool_id,
        config
      ) as unknown as InfiniteQueryObserverOptions),
      queryKey,
      ...(queryOptions as unknown as Omit<
        InfiniteQueryObserverOptions,
        'queryKey'
      >),
    },
    queryClient
  ) as UseInfiniteQueryResult<TData, ResponseErrorConfig<RetrieveTool422>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
